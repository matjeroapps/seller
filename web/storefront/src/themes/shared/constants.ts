/**
 * Shared DOM contract constants for storefront themes.
 *
 * `PURCHASE_CONTROL_ANCHOR` is the DOM id of the product purchase control element
 * (add to cart / buy now buttons). Theme section CTAs (like final_cta) anchor to this
 * element to hand over interactive purchase flow without duplicating cart logic.
 */
export const PURCHASE_CONTROL_ANCHOR = 'purchase-control';
